export const instrumentSelectionInstructions = `Before composing, discover and scan available instrument plugins, then call content_discover_roots/content_index and content_search for installed libraries and instrument patches INSIDE sampler/container plugins (Kontakt, Reaktor, Battery and similar). A plugin name is not an instrument choice: identify library, patch, playing range and articulation. Search content_search and sound_search using the musical role and style, compare plausible choices by sound_probe or sound_audition_in_context, and record the exact selected preset/state and rationale. The standard setup includes AIDAW GM (FluidR3), EQ, Limiter and Reverb as normal VST3 plugins. If no suitable user-installed instruments exist, or the user requests the starter bank, explicitly select AIDAW GM under plugin_first after reviewing its programs. This is an authorized starter option, not an automatic substitution for a missing requested plugin. Never default to Apple DLS/QuickTime or other General MIDI or builtin sounds because they are convenient or because a library adapter fails. Basic sounds require an explicit user request or an explicit test/sketch purpose (instrument_policy=allow_basic). Do not relax this policy merely to bypass a load failure. A discovered file is not a playable or licensed instrument. content_index records filesystem/registration evidence, not sound quality. Do not claim that an NKI/NKSN file has loaded just because it exists. Use only a verified format adapter or saved plugin state; explain missing capability and select another suitable installed candidate without silently substituting GM. Report discovery gaps rather than treating an incomplete scan as proof a library is absent. Assess audio in the calling tool; no separate audio model is configured in AIDAW.`;
export function isBasicInstrument(s:any){return s?.kind==='builtin'||(s?.kind==='plugin'&&(/DLSMusicDevice|DLS Music Device|QuickTime|Microsoft.*GS Wavetable/i.test(s.plugin_id)));}
