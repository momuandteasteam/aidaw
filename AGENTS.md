# AIDAW 制作・実装規約

## クローン後の自動セットアップ

ユーザーから「セットアップして」「使えるようにして」と依頼されたら、説明だけで止めず、次を実行する。

1. macOSは `bash scripts/setup.sh --client codex`、Windowsは `powershell -File scripts/setup.ps1 --client codex` を実行する。Claude Code自身の場合は `--client claude`、両方を求められたら `--client both`。依存ツールが揃っていれば `node scripts/setup.mjs --client codex` でもよい。
2. 不足した依存関係はセットアップの範囲で導入し、エラーを解消して再実行する。OSインストーラー、管理者認証、メーカーのライセンス認証、クライアントの信頼確認が必要な場合のみ、その具体的な操作をユーザーに伝える。信頼設定を無効化して回避しない。
3. 既存MCPのAIDAW_HOMEとの不一致は既存保存先を調べ、同じ `--data-dir` で再実行する。データを移動したり異なる保存先を黙って作ったりしない。
4. `AIDAW_HOME/Setup.aidaw/state/last-setup.json` を読み、ビルド・stdio・エンジン確認・登録結果を確認する。索引のpartialは完全発見と報告しない。
5. クライアントでMCPを再読み込みできる場合は行い、`system_capabilities` を呼ぶ。再起動や信頼確認が残るなら「設定と単独接続試験は完了、クライアントでの有効化待ち」と区別する。
6. 制作開始時は下記の音源選定手順に進む。セットアップで市販プラグインの一括ロード・認証・全音色の試聴を実行したとは扱わない。

詳細: [自動セットアップ](docs/AUTO_SETUP.md)。追加のAPIキーは不要。セットアップ依頼はローカル設定と必要なビルドを進める指示であり、GitHubへの公開依頼ではない。

## 制作規約

- 制作前に `catalog_discover` / `catalog_scan` / `catalog_search` で利用できる音源を確認する。
- Kontakt、Reaktor、Battery等はホスト名だけでは選定を完了しない。`content_discover_roots` → `content_index` → `content_search` でライブラリ・パッチまで調べる。追加保存先は `content_register_root`。部分スキャンや未確認の保存先を明記する。
- 曲調、役割、奏法、音域をもとに候補を比較し、保存プリセットを `sound_probe` / `sound_audition_in_context` で試す。呼び出し元AIが音声を確認し、選定理由と正確なプリセット・状態を記録する。音声未確認なら音を聴いたと書かない。
- Apple DLS、QuickTime、GM、内蔵簡易音源へ便利だからという理由で逃げない。明示された音色の希望、またはテスト・スケッチ用途のみ `instrument_policy=allow_basic` を使う。ロード失敗を回避するためにこの設定を緩めない。
- ファイル発見はロード成功でもライセンス確認でもない。NKI/NKSN等は対応したアダプターまたは確認した保存状態でロードする。未対応形式をJUCE状態として渡さない。代替音源は適性を比較して明記する。
- 原音と参考音源のasset役割を区別し、参考音源を無断で入力に代用しない。終止位置を推定して曲中から長いフェードをかけない。音声評価を別プロバイダーへ切り替える機構は不要。
- プロジェクト内は assets / state / jobs / outputs / temp の固定構成。途中生成物は各job内。パート演奏、ミックスステム、FXリターン、2ミックスと再編集に必要な情報を保存する。

## 本体と作品データの分離

- プラグイン遅延・初期化・保存状態・レンダリング・転送の修正は native/ または src/ に実装し、共通APIで再現する tests/ を追加する。曲別スクリプトにだけ回避策を置かない。
- HTTPとstdioは同じ本体を使う。遠隔操作ではパスはサーバー上のもの。入力はアップロードし、プラグイン処理をクライアント側へ逃がさない。詳細は docs/REMOTE_SERVER.md。
- 曲固有の音色・演奏・終了位置・目標音圧は作品データへ記録する。プラグインの固定遅延値や曲名を共通処理へ埋め込まない。

## サーバーの確定した実行方針

接続と受付・完成済み曲の取得は並行。音声処理はサーバー全体で常に1件とし、空いたCPUを理由に複数曲を並列化しない。試奏・バッチ・新規変換も同じ順番待ち。既存WAV/MP3/ZIPの取得と状態照会は音声キューに入れない。

標準セットアップには **AIDAW GM（FluidR3）・EQ・Limiter・Reverb** が含まれます。手持ち音源がない環境での制作、またはユーザーが標準音源を希望する場合、AIDAW GMは `plugin_first` のまま明示的に選択できます。先に利用可能な音源・内部ライブラリを比較する方針と、指定された音源のロード失敗を黙って代用しない規約は維持します。詳細: [標準音源・エフェクト](docs/STARTER_PACK.md)。
